<section id="work" class="ws">
  <div class="ws-sep" aria-label="010010010101001010100101">
    <span>0</span><span>1</span><span>0</span><span>0</span><span>1</span><span>0</span>
    <span>0</span><span>1</span><span>0</span><span>1</span><span>0</span><span>1</span>
    <span>0</span><span>0</span><span>1</span><span>0</span><span>1</span><span>0</span>
    <span>1</span><span>0</span><span>0</span><span>1</span><span>0</span><span>1</span>
  </div>
  <div class="ws-label">> WORK</div>
  <div class="ws-sticky">
    <div class="ws-grid" id="ws-grid">
      <button class="ws-back" id="ws-back">←</button>
      <div class="ws-item" data-col="1" data-row="1" data-expand="photo" data-r1="Neuromorphic Computing Cryptography" data-r2="_> Python _> React _> PostgreSQL _> Flask API _> snnTorch _> Neuromorphic Computation _> [PUF, STDP, Spike Permutation] _> Cryptography _> [AES-256-CBC (PKCS7), AES-256-CTR, ChaCha20]" data-r3="AR3LYX1 (Andry)" data-r4="github.com/ArelyX1/neuromorphic-computing-cryptography" style="--bg:#1f2328; --fg:#ffffff;">
        <div class="ws-front"><h3 style="font-size:clamp(0.65rem, 1.1vw, 0.95rem); letter-spacing:0.08em; line-height:1.3;">Neuromorphic Computing<br>Cryptography</h3></div>
        <div class="ws-expand">photo</div>
      </div>
      <div class="ws-item" data-col="2" data-row="1" data-expand="pro2" style="--bg:#70757f; --fg:#ffffff;">
        <div class="ws-front"><h3>010</h3></div>
        <div class="ws-expand"></div>
      </div>
      <div class="ws-item" data-col="3" data-row="1" data-expand="pro3" style="--bg:#c02f4a; --fg:#ffffff;">
        <div class="ws-front"><h3>011</h3></div>
        <div class="ws-expand"></div>
      </div>
      <div class="ws-item" data-col="1" data-row="2" data-expand="pro4" style="--bg:#b9bcc2; --fg:#1f2328;">
        <div class="ws-front"><h3>100</h3></div>
        <div class="ws-expand"></div>
      </div>
      <div class="ws-item" data-col="2" data-row="2" data-expand="pro5" style="--bg:#8a94a3; --fg:#ffffff;">
        <div class="ws-front"><h3>101</h3></div>
        <div class="ws-expand"></div>
      </div>
      <div class="ws-item" data-col="3" data-row="2" data-expand="pro6" style="--bg:#f7f5f2; --fg:#1f2328;">
        <div class="ws-front"><h3>110</h3></div>
        <div class="ws-expand"></div>
      </div>
    </div>
  </div>
</section>

<style>
  .ws {
    font-family: 'Ubuntu', monospace;
  }
  .ws-label {
    text-align: center;
    font-size: clamp(1rem, 2.5vw, 1.8rem);
    font-weight: 700;
    letter-spacing: 0.4em;
    text-transform: uppercase;
    color: #444;
    padding: 0.5rem 0 1.5rem;
    font-family: 'VT323', 'Courier New', monospace;
  }
  .ws-sep {
    position: relative;
    text-align: center;
    font-size: clamp(0.6rem, 1.3vw, 1rem);
    font-weight: 700;
    letter-spacing: 0.8em;
    padding: 6rem 1.5em 0.5rem;
    font-family: 'VT323', 'Courier New', monospace;
    color: #8a0606;
    animation: vhs-jitter 0.08s infinite;
    text-shadow:
      2px 0 #ff004066,
      -2px 0 #00f0ff66,
      0 0 6px #8a060633;
  }
  .ws-sep::after {
    content: '';
    position: absolute;
    inset: 0;
    background: repeating-linear-gradient(
      0deg,
      transparent,
      transparent 2px,
      rgba(255,255,255,0.04) 2px,
      rgba(255,255,255,0.04) 4px
    );
    pointer-events: none;
    z-index: 2;
    animation: scanlines 0.1s infinite linear;
  }
  .ws-sep span {
    display: inline-block;
    position: relative;
    transition: color 0.05s;
  }
  .ws-sep span:nth-child(odd) {
    color: #d40808;
    text-shadow:
      1px 0 #ff004088,
      -1px 0 #00f0ff88;
  }
  .ws-sep span:nth-child(even) {
    color: #9a0707;
    text-shadow:
      -1px 0 #ff004066,
      1px 0 #00f0ff66;
  }
  @keyframes vhs-jitter {
    0% { transform: translate(0, 0); }
    10% { transform: translate(-1px, 0.5px); }
    20% { transform: translate(1px, -0.3px); }
    30% { transform: translate(-0.5px, 0.8px); }
    40% { transform: translate(0.5px, -0.5px); }
    50% { transform: translate(0, 0); }
    60% { transform: translate(-0.8px, 0.3px); }
    70% { transform: translate(0.8px, -0.8px); }
    80% { transform: translate(-0.3px, 0.5px); }
    90% { transform: translate(0.3px, -0.3px); }
    100% { transform: translate(0, 0); }
  }
  @keyframes scanlines {
    0% { background-position: 0 0; }
    100% { background-position: 0 4px; }
  }
  .ws-sticky {
    height: 130vh;
  }
  .ws-grid {
    position: sticky;
    top: 0;
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    grid-template-rows: 1fr 1fr;
    width: 100%;
    height: 100vh;
    gap: 2px;
    background: #e0e0e0;
  }
  .ws-item {
    position: relative;
    overflow: hidden;
    cursor: pointer;
    background: var(--bg);
    color: var(--fg);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1;
  }
  .ws-item.expanding {
    z-index: 10;
    cursor: default;
  }
  .ws-front {
    text-align: center;
    pointer-events: none;
    transition: opacity 0.3s ease;
  }
  .ws-front h3 {
    margin: 0;
    font-size: clamp(2rem, 5vw, 4rem);
    font-weight: 900;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    transition: opacity 0.3s ease;
  }
  .ws-front.small h3 {
    font-size: clamp(0.55rem, 1.2vw, 0.9rem);
    font-weight: 500;
    letter-spacing: 0.1em;
    text-align: left;
  }
  .ws-expand {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: clamp(1.5rem, 4vw, 3rem);
    font-weight: 900;
    letter-spacing: 0.3em;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.3s ease;
    z-index: 3;
  }
  .ws-expand.visible {
    opacity: 1;
  }
  .ws-back {
    display: none;
    position: absolute;
    top: 1rem;
    left: 1rem;
    z-index: 20;
    background: rgba(0,0,0,0.35);
    border: 2px solid #ffffff;
    border-radius: 50%;
    width: 3rem;
    height: 3rem;
    font-size: 1.2rem;
    cursor: pointer;
    color: #ffffff;
    transition: opacity 0.3s ease;
    align-items: center;
    justify-content: center;
  }
  .ws-back.visible {
    display: flex;
  }
  .ws-back:hover {
    opacity: 0.6;
  }
</style>
